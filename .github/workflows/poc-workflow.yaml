name: POC workflow
on:
  push:
    branches:
      - main
  pull_request:

jobs:
  poc:
    name: "hello world"
    runs-on: ubuntu-latest
    steps:
      - name: echo
        run: "echo 'hello-world'"
